# most of my env files contain secrets, so ignore them by default
*.env
# and I don't want any of fetch committed
fetch/
# this whole folder is generated by hugo, and shouldn't be committed
write.markgius.com/
# jitsi default compose binds this directory and a bunch of crap into it, but there's really no reason to do that so I'll just ignore before I volumize it.
.jitsi-meet-cfg/
# TODO: move these into a volume as well
etc-dnsmasq.d/
etc-pihole/
